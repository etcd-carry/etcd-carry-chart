apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd-carry.fullname" . }}
  labels:
    {{- include "etcd-carry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etcd-carry.selectorLabels" . | nindent 4 }}
