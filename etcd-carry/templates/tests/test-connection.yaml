apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etcd-carry.fullname" . }}-test-connection"
  labels:
    {{- include "etcd-carry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etcd-carry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
